propel:
  product:
    _attributes: { phpName: product }
    id: { type: integer, autoIncrement: true, required: true, size: 11, primaryString: true }
    name: { type: varchar, size: 255 }
    price: { type: double }
    quantity: { type: integer }
    image: { type: longvarchar }

  color:
    _attributes: { phpName: color }
    id: { type: integer, autoIncrement: true, required: true, size: 11, primaryString: true }
    name: { type: varchar, size: 255 }    
      
  dummy:
    _attributes: { phpName: Dummy }
    id: { type: integer, autoIncrement: true, required: true, size: 11, primaryString: true }
    combo: { type: varchar }
